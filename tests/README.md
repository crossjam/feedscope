# Tests

This directory holds pytest-based coverage for the feedscope CLI and supporting modules. Run the suite with:

```bash
uv run pytest
```

Add new tests alongside the CLI subcommands they exercise.
